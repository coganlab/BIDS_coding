.. The main data2bids converter guide. Each task has its own specific needs, and therefore has extending guides that can be found in the `tasks` folder.

Data2BIDS
=========

.. argparse::
   :prog: data2bids.py
   :module: BIDS_converter.data2bids
   :func: get_parser


Each task has its own specific needs, and therefore has extending guides. Each task has it's own branch on github.

.. toctree::
    :maxdepth: 2
    :caption: Tasks

    GlobalLocal/index